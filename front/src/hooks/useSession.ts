import type { Session } from "../core/models/Session";
import type { Solve } from "../core/models/Solve";

export function useSession() {
  // Sessions, activeSessionId

  // CUD Session, Add/Delete Solve

  // useEffect : Load/Save localStorage
}